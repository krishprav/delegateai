{
  "name": "n8n",
  "private": true,
  "scripts": {
    "build": "bun run db:generate && turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:migrate": "cd packages/db && bun x prisma migrate dev && bun x prisma generate",
    "db:generate": "cd packages/db && bun x prisma generate",
    "start": "turbo run start",
    "start:frontend": "cd apps/frontend && bun run start",
    "start:backend": "cd apps/backend && bun run start",
    "start:engine": "cd apps/engine && bun run start"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "dotenv": "^17.2.3",
    "gsap": "^3.14.2",
    "motion": "^12.29.0"
  }
}